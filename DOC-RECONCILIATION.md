# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the
code as read-only ground truth and edits only documentation so it reflects the repo's actual current
state. It changed no source code and ran no builds or tests.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`

**Evidence:** `verified-by-reading-code`

README intro accurately describes a 100% local RAG system with 4 interfaces (CLI, REST API, Web
dashboard, MCP server). All described capabilities are present in source: loaders
(`src/rag_kb/ingestion/loaders/`), chunkers (`src/rag_kb/ingestion/chunkers/`), hybrid search
(`src/rag_kb/retrieval/engine.py`), BM25 (`src/rag_kb/ingestion/bm25_store.py`), reranker
(`src/rag_kb/retrieval/reranker.py`), generation (`src/rag_kb/generation/engine.py`). Entry points
verified in `pyproject.toml:38-41` (`rag` CLI, `rag-kb-mcp` MCP server). Swagger UI URL
(`http://127.0.0.1:8000/api/docs`) confirmed at `src/rag_kb/api/__init__.py:59`.

---

### 2. Current state

**Status:** `drifted` → **fixed in `CLAUDE.md`**

**Evidence:** `verified-by-reading-code`

`CLAUDE.md` "Current Phase" section showed Phase 0 with eight unchecked boxes ("Nothing built yet"
semantics). Code evidence contradicts this:

- 40+ test files across `tests/` and `tests/integration/`
- 16 REST endpoints in `src/rag_kb/api/routes.py:1`
- 12 MCP tools in `src/rag_kb/mcp/__init__.py:1`
- 7 React pages in `web/src/pages/`
- Git history: `Merge feat/phase1-backend-hardening: Phases 1-4 complete` and
  `Merge feat/phase5-e2e-docs: Phase 5 E2E tests, Docker deployment, MCP packaging, docs`

**Change made (`CLAUDE.md`):**
- Before: `**Phase 0: Foundation** (target: Weeks 1-2)` with 8 unchecked boxes
- After: `**All phases complete** (as of commit 5a036aa, 2026-05-30)` with 6 checked phase lines
  summarising phases 0-5

---

### 3. Stack

**Status:** `drifted` → **fixed in `CLAUDE.md`** (one field); otherwise `consistent`

**Evidence:** `verified-by-reading-code`

`CLAUDE.md` listed `tree-sitter 0.22+` but `pyproject.toml:14` declares `tree-sitter>=0.24`.
All other stack claims (Python 3.12, FastAPI >=0.115, Qdrant >=1.13, aiosqlite >=0.20,
PyMuPDF >=1.24, Click >=8.1, FastMCP >=2.0, sentence-transformers in reranker group, React 19 +
Vite + Tailwind, Docker Compose) are consistent with `pyproject.toml` and `web/package.json`.

**Change made (`CLAUDE.md`):**
- Before: `- **Code Parsing:** tree-sitter 0.22+`
- After: `- **Code Parsing:** tree-sitter 0.24+`

---

### 4. How to run

**Status:** `drifted` → **fixed in `README.md`** (one comment); otherwise `consistent`

**Evidence:** `verified-by-reading-code`

All Makefile targets referenced in the docs exist in `Makefile:1-82` (`dev`, `dev-web`, `dev-mcp`,
`init-db`, `test`, `test-integration`, `lint`, `format`, `type-check`, `check`, `prod-up`,
`prod-down`, `prod-logs`, `health`, `logs-api`, `reset-db`). One mismatch: `README.md` described
`make check` as running "All of the above" (which would include `format`), but `Makefile:69`
defines `check: lint type-check test` — `format` is excluded.

**Change made (`README.md`):**
- Before: `make check             # All of the above`
- After: `make check             # lint + type-check + test (not format)`

---

### 5. Known risks

**Status:** `consistent`

**Evidence:** `verified-by-reading-code`

No doc↔code contradictions in user-facing risk claims. API response envelope, error codes, rate
limit headers, and authentication flow in `docs/API.md` match `src/rag_kb/api/routes.py`,
`src/rag_kb/api/auth.py`, and `src/rag_kb/api/rate_limit.py`. Docker production architecture in
`docs/DEPLOYMENT.md` matches `docker-compose.prod.yml`. MCP tool count ("12 tools") matches
`src/rag_kb/mcp/__init__.py` (12 `@mcp.tool` decorators). REST endpoint count ("16 endpoints")
matches `src/rag_kb/api/routes.py` (16 `@router.*` route handlers).

---

### 6. Next move

**Status:** `drifted` → **fixed via Claim 2 edit to `CLAUDE.md`**

**Evidence:** `verified-by-reading-code`

"Current Phase" checkboxes suggested the entire project remained unbuilt. With all phases marked
complete, there is no pending roadmap work visible in the code. Future work (web scraping, knowledge
graph, agentic multi-hop retrieval) is correctly deferred in `DISCOVERY-SUMMARY.md:47-53`.

---

## Contradictions for Manual Review

These files contain stale content but are outside the editable set (`README.md`, `CLAUDE.md`,
`AGENTS.md`, `DOC-RECONCILIATION.md`, `docs/`). A human should update them.

| File | Location | What is wrong | Suggested fix |
|------|----------|---------------|---------------|
| `RESUMPTION-PROMPT.md` | lines 13–14 | `"Last completed phase: None (fresh start)"` / `"Nothing yet. Fresh project."` — all phases are complete | Update "Current State" and "What's Already Built" to reflect the completed system |
| `IMPLEMENTATION-ROADMAP.md` | lines 205–215 | Session 9 describes the API as split into `routes/collections.py`, `routes/documents.py`, `routes/ingest.py`, `routes/search.py`, `routes/admin.py` — actual implementation is a single `src/rag_kb/api/routes.py` | Update the file list to `routes.py` (single-file router) |

---

## Footer

Generated: 2026-05-30 04:18:36 PDT
Branch: `docs/truth-up-2026-05-30`
HEAD sha reconciled against: `5a036aa80000ec3083b33b290a11fb48744cd7b5`
